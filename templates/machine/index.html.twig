{% extends 'base.html.twig' %}

{% block title %}Machine index{% endblock %}

{% block body %}
    <h1>Machine index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marque</th>
                <th>Modele</th>
                <th>Nb_heure</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for machine in machines %}
            <tr>
                <td>{{ machine.id }}</td>
                <td>{{ machine.marque }}</td>
                <td>{{ machine.modele }}</td>
                <td>{{ machine.nbHeure }}</td>
                <td>
                    <a href="{{ path('machine_show', {'id': machine.id}) }}">show</a>
                    <a href="{{ path('machine_edit', {'id': machine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('machine_new') }}" class="btn btn-primary">Create new</a>
{% endblock %}
